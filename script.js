alert('olá. estou aqui em cima.');

function userlogin(user, senha) {
    return {
        user: function (user) {
            captura de usuario.
        },
        login: function (login) {
            captura de logindouser.
        }
    };
}
